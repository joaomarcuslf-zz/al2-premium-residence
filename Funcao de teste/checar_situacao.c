#include <stdio.h>
#define true 1
#define false 0
#define and &&

int main() {
  int checar;
  int checar_situacao(int, int, int, char);
  checar = checar_situacao(true, 405)
}

int checar_situacao(int aval, int num,, int bloco, char sit) {
  if(aval==true and apartamento[bloco-1][(num/100)-1][num%10]=='D') {
    apartamento[bloco-1][(num/100)-1][num%10]=sit;
    return true;
  }
  else if(aval==false) {
    return false;
  }
  else {

  }

}
